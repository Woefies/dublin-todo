{
  "name": "dublin-todo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run check && vite build",
    "preview": "vite preview",
    "check": "node scripts/check-pois.mjs && node scripts/check-filters.mjs && node scripts/check-search.mjs && node scripts/check-urlstate.mjs && node scripts/check-enrich.mjs"
  },
  "dependencies": {
    "maplibre-gl": "^5.0.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
